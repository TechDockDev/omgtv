# GCP Project
GCP_PROJECT_ID=pocketlol-68ca6

# GCS Buckets (existing bucket with MP4s and hls/ folder)
GCS_UPLOADS_BUCKET=videos-bucket-pocketlol
GCS_STREAMING_BUCKET=videos-bucket-pocketlol

# Pub/Sub (using existing topics)
PUBSUB_SUBSCRIPTION=uploaded-media-sub
PUBSUB_READY_TOPIC=streaming-audit

# CDN Base URL
CDN_BASE_URL=https://stream.cdn.pocketlol

# Transcode settings
TRANSCODE_TEMP_DIR=/tmp/transcode
HLS_SEGMENT_DURATION=4

# Upload Service callback (optional - using Pub/Sub instead)
# UPLOAD_SERVICE_URL=http://upload-service:5000
# SERVICE_AUTH_TOKEN=change-me

# Logging
LOG_LEVEL=info
